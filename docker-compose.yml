services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./src:/app/src
      - ./output:/app/output
      - ./resources:/app/resources
      - ${APP_DIR:-./app_data}:/app/app_data
      # Mount cache directory to persist models
      - ~/.cache/huggingface:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
    command: uvicorn main:app --host 0.0.0.0 --port 8000
